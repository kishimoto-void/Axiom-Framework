[package]
name = "pss"
version = "1.0.0-rc1"
edition = "2021"
authors = ["kishimoto-void"]
description = "Problem Specification Standard (PSS) - Production Reference Implementation for Axiom Framework"
license = "Non-Commercial / Research Use Only"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[[bin]]
name = "pss-demo"
path = "src/main.rs"
